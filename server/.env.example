DATABASE_URL="postgresql://4rmtech:4rmtech_password@localhost:5432/4rmtech?schema=public"
PORT=4000
JWT_SECRET="change-me-to-a-long-random-string"
FRONTEND_ORIGIN="http://localhost:5173"

# Firebase Admin: required for GET /api/auth/firebase-custom-token (Firestore cart/checkout).
# Option A — paste the full service account JSON as a single line:
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"rmtech-68ce4",...}
# Option B — path to the JSON file (also works with admin.initializeApp()):
# GOOGLE_APPLICATION_CREDENTIALS=C:/path/to/serviceAccount.json

